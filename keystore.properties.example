# Copy to keystore.properties (gitignored) and fill in with the SAME upload key
# the Expo/EAS builds were signed with, otherwise Play rejects the update.
storeFile=credentials/android/keystore.jks
storePassword=
keyAlias=
keyPassword=
